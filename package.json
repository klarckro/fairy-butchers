{
  "name": "fairy-butchers",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tinacms build && nuxt generate",
    "dev": "tinacms dev -c \"nuxt dev\"",
    "generate": "tinacms build && nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "@nuxt/content": "^3.14.0",
    "@nuxtjs/i18n": "^10.4.0",
    "@tinacms/cli": "^2.3.1",
    "better-sqlite3": "^12.10.0",
    "nuxt": "^4.4.6",
    "tinacms": "^3.8.1",
    "vue": "^3.5.34",
    "vue-router": "^5.0.7"
  }
}
